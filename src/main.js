import PhoneNumberInput from "./components/PhoneNumberInput";

export default {
  // eslint-disable-next-line no-unused-vars
  install(Vue, options) {
    Vue.component("simple-phone-number-input", PhoneNumberInput);
  }
};
